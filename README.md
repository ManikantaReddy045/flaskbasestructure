# flaskbasestructure
1. Export the floder 
  export FLASK_APP=flaskr
2. command for flask migrate in case if modify the model
  flask db init
  flask db migrate 
  flask db upgrade

3.To Run
  flask --app flaskr run
